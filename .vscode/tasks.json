{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_debug",
            "type": "shell",
            "command": "cd build && cmake -GNinja -DCMAKE_BUILD_TYPE=Debug .. && cmake --build . -j8"
        }
    ]
}